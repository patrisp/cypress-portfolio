/// <reference types="cypress" />

describe('Verify that Matchmaking feature is working correctly', () => {

    beforeEach(() => {

    });

    context('User profile or preferences are not completed', () => {

        it('Pop-up is displayed if neither user profile nor preferences are completed', () => {

        });
    
        it('User is redirected to profile or preferences from the pop-up', () => {
    
        });
    });

   context('User profile or preferences are completed', () => {
        
        it('Pop-up is not displayed if user profile or preferences are completed', () => {

        });
    
        it('A list of matching offers is displayed', () => {
    
        });
    });

});